# K3s worker server: runs apps, and consumes storage from the network (via iSCSI and/or NFS)
imageName: 'server-k3s'
baseImage: 'server-worker' # ../server-worker
baseImages:
  - 'alma-linux-10'
apps:
  - 'k3s'
