# Worker server with ZFS: runs apps, and consumes storage from the network (via iSCSI and/or NFS)
imageName: 'server-worker-zfs'
baseImage: 'server-zfs' # ../server-zfs
architectures:
  - amd64
baseImages:
  - 'alma-linux-10'
apps: []
